platform :ios, '10.0'

target 'LibraryApp' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Library API
  pod 'LibraryAPI', git: 'git@github.com:anderscarling/library_presentation-protobuf.git'
  pod 'Alamofire'

end
